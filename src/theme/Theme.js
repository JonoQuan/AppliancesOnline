import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#03658C'
        },
        secondary: {
            main: '#F24141',
        },
    },
    typography: {
        fontFamily: 'Rubik'
    }
});

export default theme;