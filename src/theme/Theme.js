import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#90caf9'
        },
        secondary: {
            main: '#d1c4e9',
        },
    },
    typography: {
        fontFamily: 'Rubik'
    }
});

export default theme;